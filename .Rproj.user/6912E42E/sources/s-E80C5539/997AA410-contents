# This function will export the area-lookup tables and rasters of habitat area by discharge
# Last edited by Elaina Passero on 10/28/19


exp.fish.habitat <- function(outputs,species,lifestages,){
  
  
  
  write.csv(area_look_tab, file = paste(reach_wd, "dss_outputs/",fish, "_", a, "_aqlookup.csv", sep = ""))
  
  if(exportRast == "Yes"){
    writeRaster(open_brick[n],
                filename = paste(reach_wd, "dss_outputs/", sort_q[i], "_", fish, "_", a, "_", "hab.tif", sep = ""), 
                format = GTiff)
  }
  
  
  
}