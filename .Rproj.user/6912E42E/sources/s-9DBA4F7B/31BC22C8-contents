# This function pulls riparian vegetation logistic regression equations for a single group
# Last edited by Elaina Passero on 11/20/19

find.veg.logit <- function(all_veg_logit,v){
  col_num <- grep(v,names(all_veg_logit),ignore.case = TRUE)
  titles <- names(all_veg_logit)
  one_veg_logit <- all_veg_logit[col_num]
  return(one_veg_logit)
}
